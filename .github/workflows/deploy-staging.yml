name: Deploy Staging

on:
  push:
    branches: [develop]

jobs:
  deploy-web:
    name: Deploy Web (Vercel)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Placeholder
        run: echo "Vercel auto-deploys via GitHub integration. This workflow is for future custom deploy steps."

  deploy-ai:
    name: Deploy AI Service
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Placeholder
        run: echo "TODO - AI service deploy (인프라 구축 후 활성화)"
